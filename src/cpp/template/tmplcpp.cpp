// HEADER
// title: Template for ICPC
// shortcut: tmplicpc
// description: ICPC用のテンプレート
// template: true
// author: keymoon

// BODY

#include<bits/stdc++.h>
using ll = long long;
#define var auto
const char newl = '\n';

using namespace std;

template<typename T1, typename T2> inline void chmin(T1& a, T2 b) { if (a > b) a = b; }
template<typename T1, typename T2> inline void chmax(T1& a, T2 b) { if (a < b) a = b; }

int main() {
  ios::sync_with_stdio(false); cin.tie(nullptr);
  cout << fixed << setprecision(x); cerr << fixed << setprecision(x);
  /*cursor*/
}
